name: boozin
version: 1.0.0+1
publish_to: none
description: A new Flutter project.
environment:
  sdk: ">=2.19.2 <3.0.0"

dependencies:
  cupertino_icons: ^1.0.2
  get: ^4.6.5
  progress_indicator: ^0.2.2
  flutter_svg: ^2.0.4
  google_fonts: ^4.0.3
  health: ^4.6.0
  permission_handler: ^10.2.0
  shimmer_animation: ^2.1.0+1

  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: 2.0.1
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/images/splash/
    - assets/images/splash/light/
    - assets/images/splash/dark/
